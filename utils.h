#ifndef __UTILS_H__
#define __UTILS_H__

#include <vector>
#include <string>

using namespace std;

vector<string> words(const string &str);

// for debug
void cat(string filename);

#endif  // __UTILS_H__
